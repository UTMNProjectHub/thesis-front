version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
    container_name: thesis-front
    ports:
      - '80:80'
    networks:
      - thesis-network
    restart: unless-stopped

networks:
  thesis-network:
    driver: bridge
