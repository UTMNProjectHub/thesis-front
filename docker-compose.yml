version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
    container_name: thesis-front
    ports:
      - '5123:80'
    env_file:
      - .env
    networks:
      - thesis-network
    restart: unless-stopped

networks:
  thesis-network:
    driver: bridge
